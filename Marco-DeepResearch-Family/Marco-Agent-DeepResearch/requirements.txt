# Core dependencies
openai>=1.0.0
pyyaml>=6.0
tqdm>=4.65.0

# Tool dependencies (Search / Visit)
requests>=2.28.0
markdownify>=0.11.6
PyPDF2>=3.0.0

# Token counting (optional but recommended)
tiktoken>=0.5.0

# Custom tokenizer support (optional, for models not in tiktoken)
transformers>=4.30.0
