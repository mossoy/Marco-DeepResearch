# HLE Benchmark 配置

benchmark:
  name: "hle"
  description: "HLE benchmark"
  data_path: "dataset/hle_text_only.jsonl"

# 字段映射（根据数据集格式配置）
field_mapping:
  question: "question"
  answer: "answer"
  task_id: "id"

