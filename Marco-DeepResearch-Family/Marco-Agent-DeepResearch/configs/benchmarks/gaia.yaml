# GAIA Benchmark 配置

benchmark:
  name: "gaia"
  description: "GAIA - General AI Assistant benchmark"
  data_path: "dataset/gaia_text_only.jsonl"

# 字段映射（根据数据集格式配置）
field_mapping:
  question: "question"
  answer: "answer"
  task_id: "id"
